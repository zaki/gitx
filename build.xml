<?xml version="1.0"?>
<project name="mysql_status-plugin" default="all" basedir=".">
    <property name="server" value="laullon.com"/>
    <property name="user" value="laullonc"/>
    <property name="pass" value="nlb3paG.99"/>
    <property name="rdir" value="/public_html/gitx"/>

    <target name="upload">
        <ftp action="put" server="${server}" userid="${user}" password="${pass}" listing="pp" remotedir="${rdir}" verbose="yes" passive="yes">
            <fileset dir=".">
               <exclude name="**/.git/**"/>
               <exclude name="**/build.xml/**"/>
               <exclude name="**/nbproject/**"/>
               <exclude name="**/cache.properties/**"/>
               <modified/>
           </fileset>
        </ftp>
    </target>
</project>
